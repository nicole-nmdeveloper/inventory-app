import styled from 'styled-components'

import { primaryLighterColor } from '../../config/colors'

export const Container = styled.main`
  text-align: center;

  background: ${primaryLighterColor};

  padding: 30px;
`
